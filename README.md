# ToDo-App

メインの最新の環境を取り込む
git pull origin main

新しいブランチを作成
git checkout -b ブランチ名

ブランチ移動
git checkout ブランチ名

変更を保存
git add ファイル名

git commit -m "コメント"

リモート(git hub)に反映
git  push origin ブランチ名



docker起動コマンド
docker compose up --build
